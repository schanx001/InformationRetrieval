The archived file contains following source files:
	1. pagerank.py
	Other files are generated ones and brief reports.
	
Setup:
	1. 	Check if python 3.x is installed.
	2. 	Open the file using any python IDE like Pycharm and run the code (shift + f10) 
	OR 
		Open the IDLE python 3.5 -> open new file -> copy paste code into it -> run (cntrl + f5) OR (fn + f5)  
	3. Download following libraries if not installed:
		BeautifulSoup4
		urllib
	4. For output the python code files generates output files which can be compared with the already generated files in the folder. 	
	5. The filename for the 2 graphs is mentioned in the code and commented as only on graph can be used while running the program.
	6. The output filename is also mentioned in the code to compare it with already generated output according to the graph they represent.
	
Citation:
	Referred Documentations of: 
		Python 
		BeautifulSoup4
	Referred working of dictionary and the functions that can be called onto it by following links:
		https://www.tutorialspoint.com/python/python_dictionary.htm
		https://docs.python.org/2/library/stdtypes.html
		http://stackoverflow.com/questions/3199171/append-multiple-values-for-one-key-in-python-dictionary
		http://docs.activestate.com/activepython/2.5/python/lib/defaultdict-examples.html
		https://docs.python.org/3/tutorial/datastructures.html
		
		